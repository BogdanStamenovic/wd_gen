schema: 1
name: wd_gen
description: Targeted OSINT/CTF username and password wordlist generator from a target profile
homepage: https://github.com/BogdanStamenovic/wd_gen
tags: [cli, osint, wordlist, password, ctf, python]
install:
  platforms: [linux, darwin, windows]
  # The optional --llm layer talks to an ollama server over HTTP (default host:
  # http://archserver:11434, override with --llm-host or $OLLAMA_HOST), so there
  # is no local model to pull — install is just the venv + package.
  setup:
    linux:
      - python -m venv .venv
      - .venv/bin/python -m pip install -e .
    darwin:
      - python -m venv .venv
      - .venv/bin/python -m pip install -e .
    windows:
      - python -m venv .venv
      - '.venv\Scripts\python.exe -m pip install -e .'
  update:
    linux: [.venv/bin/python -m pip install -e .]
    darwin: [.venv/bin/python -m pip install -e .]
    windows: ['.venv\Scripts\python.exe -m pip install -e .']
  remove: []
command:
  linux: .venv/bin/wd_gen
  darwin: .venv/bin/wd_gen
  windows: '.venv\Scripts\wd_gen.exe'
